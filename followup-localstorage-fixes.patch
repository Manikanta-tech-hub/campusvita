diff -ruN a/app/lib/api.ts b/app/lib/api.ts
--- a/app/lib/api.ts	2026-08-17 19:12:41.743974936 +0000
+++ b/app/lib/api.ts	2026-08-17 19:12:41.748158606 +0000
@@ -1,8 +1,10 @@
+import { getAccessToken } from "@/app/lib/auth";
+
 const API_URL =
   process.env.NEXT_PUBLIC_API_URL || "http://127.0.0.1:8000";
 
 export async function getDashboard() {
-  const token = localStorage.getItem("access_token");
+  const token = getAccessToken("admin");
 
   if (!token) {
     throw new Error("No access token found. Please login again.");
diff -ruN a/app/profile/page.tsx b/app/profile/page.tsx
--- a/app/profile/page.tsx	2026-08-17 19:12:41.752386928 +0000
+++ b/app/profile/page.tsx	2026-08-17 19:12:41.754123263 +0000
@@ -21,6 +21,11 @@
 import { useRouter } from "next/navigation";
 
 import Navbar from "@/components/layout/Navbar";
+import {
+  getAccessToken,
+  setSessionField,
+  clearAuthSession,
+} from "@/app/lib/auth";
 
 const API_URL = "http://127.0.0.1:8000";
 
@@ -212,17 +217,7 @@
   ============================================================ */
 
   const clearAuth = useCallback(() => {
-    localStorage.removeItem("access_token");
-    localStorage.removeItem("refresh_token");
-    localStorage.removeItem("token_type");
-    localStorage.removeItem("expires_in");
-
-    localStorage.removeItem("isLoggedIn");
-
-    localStorage.removeItem("userEmail");
-    localStorage.removeItem("email");
-    localStorage.removeItem("userName");
-    localStorage.removeItem("userRole");
+    clearAuthSession("user");
   }, []);
 
   /* ============================================================
@@ -263,10 +258,7 @@
     let cancelled = false;
 
     async function loadProfile() {
-      const token =
-        localStorage.getItem(
-          "access_token"
-        );
+      const token = getAccessToken("user");
 
       if (!token) {
         router.replace("/login");
@@ -391,21 +383,24 @@
         ------------------------------------------------------ */
 
         if (liveProfile.email) {
-          localStorage.setItem(
+          setSessionField(
             "userEmail",
-            liveProfile.email
+            liveProfile.email,
+            "user"
           );
 
-          localStorage.setItem(
+          setSessionField(
             "email",
-            liveProfile.email
+            liveProfile.email,
+            "user"
           );
         }
 
         if (liveProfile.name) {
-          localStorage.setItem(
+          setSessionField(
             "userName",
-            liveProfile.name
+            liveProfile.name,
+            "user"
           );
         }
       } catch (error) {
@@ -443,10 +438,7 @@
   ============================================================ */
 
   const updateProfile = async () => {
-    const token =
-      localStorage.getItem(
-        "access_token"
-      );
+    const token = getAccessToken("user");
 
     if (!token) {
       router.replace("/login");
@@ -656,21 +648,24 @@
         profile.email;
 
       if (savedName) {
-        localStorage.setItem(
+        setSessionField(
           "userName",
-          savedName
+          savedName,
+          "user"
         );
       }
 
       if (savedEmail) {
-        localStorage.setItem(
+        setSessionField(
           "userEmail",
-          savedEmail
+          savedEmail,
+          "user"
         );
 
-        localStorage.setItem(
+        setSessionField(
           "email",
-          savedEmail
+          savedEmail,
+          "user"
         );
       }
 
@@ -750,10 +745,7 @@
        AUTH
     ---------------------------------------------------------- */
 
-    const token =
-      localStorage.getItem(
-        "access_token"
-      );
+    const token = getAccessToken("user");
 
     if (!token) {
       router.replace("/login");
